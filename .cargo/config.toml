# switch-patch: cross-build config for the bit_rev lib.
[build]
target = "aarch64-nintendo-switch"

[unstable]
build-std = ["std", "panic_abort"]
build-std-features = ["compiler-builtins-mem"]

[target.aarch64-nintendo-switch]
rustflags = [
  "-C", "relocation-model=pic",
]
